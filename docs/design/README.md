# Design Artifacts

This directory contains design reference files used during site implementation.

## Files

| File | Description |
|------|-------------|
| `HMPMarketingSite.jsx` | April 17 2026 JSX mockup by Claude — the original design artifact used as the source of truth for Phase 2 component layout and copy. Do not delete; useful for diffing future redesigns. |

## Notes

- These files are **read-only reference** — do not import or execute them in the production build.
- The production source of truth is the components in `components/` and tokens in `lib/brand.ts`.
- Brand assets (logo, wordmark, headshot) live in `public/brand/` and `public/founder/`, not here.
